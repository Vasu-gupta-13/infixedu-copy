import 'package:flutter/material.dart';

class StudentAttendance extends StatelessWidget {
  const StudentAttendance({super.key, required id, String? token});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement Student Attendance Screen
    return Scaffold(
      body: Center(
        
        child:
        Text('Student Attendance Screen: TO BE MADE LATER'),),
    );
  }
}
